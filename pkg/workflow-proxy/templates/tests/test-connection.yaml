apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workflow-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "workflow-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "workflow-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
